node_modules/
dist/
coverage/
.env
.env.*.local
*.log
.DS_Store
.idea/
.vitest-cache/
!.env.example

# Agent security: local overrides and the guard audit trail stay off the repo
.claude/settings.local.json
.agent-guard.log

# Antigravity resolves hook commands from its own cwd, so the paths must be
# absolute and therefore machine-specific. Copy .agents/hooks.json.example and
# rewrite the four command paths for your clone.
.agents/hooks.json
